services:
  sueca-da-bila:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      - PORT=3002
      - NODE_ENV=production
      - NEXT_PUBLIC_URL=http://localhost:3002
    restart: always
